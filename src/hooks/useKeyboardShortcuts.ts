/** Native has no shortcut table (task 6.8 is web only); the web build's is
 * useKeyboardShortcuts.web.ts. */
export const KEYBOARD_SHORTCUTS_SUPPORTED = false

export function useKeyboardShortcuts(): void {}
